---
import Social from './Social.astro';
import Signature from './Signature.astro';
import '../styles/global.css';
import Seperator from './Seperator.astro';

// force footer to bottom of page if content height smaller than screen height
---

<footer class="footer">
    
    <div class="socialContainer">

        <Social platform="Twitter" username="camdemartini" color="var(--custom-light-brown)" hoverColor="var(--custom-white)"/>
        <Seperator height="20px" width="2px" color="var(--custom-brown)"/>
        <Social platform="GitHub" username="CameronD35" color="var(--custom-light-brown)" hoverColor="var(--custom-white)"/>
        <Seperator height="20px" width="2px" color="var(--custom-brown)"/>
        <Social platform="YouTube" username="@CameronDeMartini" color="var(--custom-light-brown)" hoverColor="var(--custom-white)"/>
        <Seperator height="20px" width="2px" color="var(--custom-brown)"/>
        <Social platform="LinkedIn" username="in/cameron-demartini" color="var(--custom-light-brown)" hoverColor="var(--custom-white)"/>

    </div>

    <Signature width="200px" color="var(--custom-light-brown)"/>
    
</footer>

<style>

    .footer {

        display: flex;
        align-items: center;
        justify-content: space-between;

        background-color: var(--custom-dark-brown);

        width: 100%;
        padding: 2em;

        transition: 0.5s var(--standard-transition-timing-no-bounce);

        /* clip-path: url(#cool-curve); */

    }

    /* footer::before {

        content: '';

        width: 100%;
        height: 100%;
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 150%;
        z-index: -2;
        background: linear-gradient(0deg, var(--custom-dark-brown) 60%, var(--custom-dark-brown-0));
                    /* url(#noiseFilter);

    } */

    #svg_filter {

        position: fixed;

    }

    .socialContainer{

        display: flex;
        align-items: center;


    }

    .fixed {

        position: fixed;
        bottom: 0%;

    }

</style>